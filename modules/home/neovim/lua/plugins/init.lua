-- Real (not example/disabled) plugins file, unlike upstream starter's
-- lua/plugins/example.lua (which is disabled by its own `if true then
-- return {} end` and not meaningful to vendor). Kept empty on purpose:
-- lazy.nvim's `{ import = "plugins" }` imports every file under this
-- directory, not just this one -- language-specific specs get their own
-- file instead (see ./ruby.lua) to keep diffs self-contained.
return {}
