-- Real (not example/disabled) plugins file, unlike upstream starter's
-- lua/plugins/example.lua (which is disabled by its own `if true then
-- return {} end` and not meaningful to vendor). Kept minimal on purpose:
-- Ruby-specific plugin specs (LSP/rubocop/treesitter/rails/rspec/dap)
-- land here in a future round, not this one -- see
-- docs/superpowers/specs/2026-08-11-neovim-base-design.md "Out of Scope".
return {}
